version: '3.9'

services:
  frontend:
    build: 
      context: ./client
      dockerfile: Dockerfile
    ports:
      - "4000:4173"

  backend:
    build:
      context: ./server
      dockerfile: Dockerfile
    ports:
      - "5000:7000"
    volumes:
      - uploads:/data/uploads
volumes:
   uploads:
